<!-- 本檔與 CLAUDE.md 內容相同，修改時請兩份同步維護。 -->

# 專案分層原則

本網站（魔力寶貝「永恆初心」攻略站）基本上分成三個部份。動手前先確認自己改的是哪一層，並遵守該層的原則。

順序上<strong>先處理好資料層的定義，再往下做畫面</strong>；畫面做到一半發現資料不夠用時，回到資料層補定義，而不是在畫面層轉換繞過。

## 1. 資料層（content/data/ 的 CSV／JSON）

定義「顯示用」的資料。

- 欄位與資料的定義盡量參考既有的；除非必要，盡量避免刪除、移除既有的欄位或資料。
- 需要修改既有欄位時，盡量透過<strong>新增欄位</strong>達成，並在註解標注哪個舊欄位有移除需求，合併時再評估是否移除。
- 覺得欄位不足需要，可自行新增欄位；不用擔心重複資料或過多資料，審核時回頭處理。

## 2. 資料來源的解析／收集

資料來源有幾種：外部攻略網站、本站的歷史內容、玩家提供的資料（例如遊戲存檔截圖），以及<strong>本站既有的資料層檔案</strong>（整併多份定義、重整格式時，同樣視為一次解析）。

- 原則上都不考慮「持續解析」的需求——解析／抓取腳本是一次性轉移，之後不會再跑，也<strong>不做任何形式的重新蓋入</strong>（`寵物合成-手動校正.md` 記錄的就是重跑蓋掉人工修正的事故）。
- 重點放在把當下拿到的資料<strong>準確地</strong>解析進資料層；轉換產出落地為資料層的穩定檔案並 commit 後，該檔案就是唯一的編輯對象。
- 所有修正一律<strong>基於當前的資料檔直接修改</strong>，不回頭改來源再重新轉換。唯一需要特別加註的情況：發現<strong>公告本身有誤</strong>而手動修正時，加上<strong>繕誤</strong>註記寫明公告原文與修正原因——確保被回報「與公告不符」時，能明確知道是公告錯了才手動改的。
- 如果來源是歷史資料、會持續新增的資料（例如公告），或發現<strong>多組性質相近的資料</strong>，可以考慮提出<strong>重組資料的提案</strong>。
- 更根本的判斷：當功能依賴的多份資料<strong>各自為其他目的維護</strong>、其欄位與假設可能隨自身需求變動而危及當前目標時，應重新拉出穩定的轉換——從來源資料做<strong>一次性轉換</strong>，產出本功能自有的穩定資料集，而不是每次建置時即時橋接。

## 3. 畫面層（src/）

- 畫面主要透過資料層的狀態反應來產生，避免直接寫 raw HTML。
- 展開的資料盡量由資料層的關係來適當地 loop 或產生決定——介面不寫死資料知識，編輯者加欄位、加列不必動程式。
- 畫面層可以由<strong>單一、穩定的資料定義</strong>推導顯示結構（loop、篩選、分組、樹展開）；但<strong>不做資料整理</strong>——多來源合併、去重、名稱正規化屬於資料層，應先一次性轉換成穩定檔案。畫面層出現這類程式碼，就是資料需要重組的訊號，回頭提出重組資料的提案。
- 要在單一頁面增加資料時，都需要仔細評估是否為該頁面所需，避免頁面資料過於複雜。

## Commit 與 PR 的切分

- 這三層的程式應該在<strong>不同 commit</strong> 中進行。
- 例外：新增資料並同時新增對應介面時，可以在同一個 commit 內同時新增資料定義與畫面。
- 其他情況請一律拆開 PR 進行。
